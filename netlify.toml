[build]
  publish = "."
  functions = "functions"

# Everything goes to the proxy function.
# We pass the original path in ?path=:splat so the function knows
# what the browser actually requested.
[[redirects]]
  from = "/*"
  to   = "/.netlify/functions/proxy?path=:splat"
  status = 200
  force = true
